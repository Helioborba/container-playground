apiVersion: v1
kind: Service
metadata:
  name: client-node-port
spec:
  type: NodePort
  ports:
    - port: 3050 # con between pods
      targetPort: 3000 # con created for the current pod
      nodePort: 31515 # con created for outside world
  selector:
    component: web
    